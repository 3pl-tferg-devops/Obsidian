These messages are generated by files in `/etc/update-modt.d/`. To stop them from being displays, you want to remove the execute flag from their permissions.

```sh
sudo chmod -x /etc/update-motd.d/*
```

